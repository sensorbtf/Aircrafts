using UI.HUD;
using UnityEngine;

namespace UI
{
    public class UIManager : MonoBehaviour
    {
        [SerializeField] private HUDManager _hud;

        public void CustomStart()
        {
            //_hud.CustomStart();
        }
    }
}